#!/usr/bin/env bash
# exit on error
set -o errexit

# Note: When using Docker, dependencies are installed in the Dockerfile
# This script is kept for non-Docker deployments or additional build steps

echo "Build script completed - Docker will handle the build"
